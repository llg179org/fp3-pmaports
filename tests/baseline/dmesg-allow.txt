# Kernel log lines that match the health check's fault patterns but are known
# noise on a healthy boot. Matched as fixed strings (grep -F), one per line.
#
# Keep this list short and justified. Every entry is a fault signature we have
# decided to stop looking at, so an unjustified line here can hide a real
# regression for months. The check prints every allowlisted line it matched,
# with its count, so an entry silences the verdict but never the evidence.

# 2026-08-16: "apcs-cpu0-pll failed to enable!" - the little cluster's CPU PLL
# times out in wait_for_pll() during an OPP change. Measured on 7.1.3-r56: 299
# instances in one 85-minute boot, the first nine seconds in, arriving roughly
# every 10-40s. ☠️ That boot also produced a "not load-driven" claim, from a
# 60s-idle-vs-30s-burn comparison whose counts came from a dmesg that had
# already wrapped - the count was known to be unreliable and the conclusion was
# drawn anyway. On the clean r57 boot the pattern is different and not yet
# explained: zero through a whole 27-check battery including an eight-core
# burn, then 24 during three acoustic audio checks. The cluster still reaches
# every OPP - policy0's
# time_in_state shows residency at all seven, up to 1804800 - so this is a
# failed rate change that gets retried, not a stuck clock, which is why nothing
# else on the phone shows it. Allowlisted so it cannot mask a second fault, NOT
# because it is benign: docs/TODO.md "apcs-cpu0-pll fails to lock, and it took
# the phone down" has the analysis, and the entry stays only because the check
# prints this line and its count on every run.
WARNING: drivers/clk/qcom/clk-alpha-pll.c:421 at wait_for_pll
